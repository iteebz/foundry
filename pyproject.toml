[project]
name = "foundry"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
]

[project.optional-dependencies]
test = ["pytest>=8.0"]
tools = ["matplotlib>=3.5", "pyyaml>=6.0"]
